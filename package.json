{
  "name": "@dolphin278/vjuga",
  "version": "7.0.0",
  "packageManager": "npm@10.5.0",
  "license": "MIT",
  "exports": {
    "./*": "./src/*"
  },
  "type": "module",
  "scripts": {
    "clean": "tsc --build --clean",
    "test": "tsc --project tsconfig.test.json && node --test 'src/tests/*.test.js'",
    "test:bun": "bun test src/tests",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "lint": "oxlint ./src",
    "lint:fix": "oxlint --fix ./src",
    "coverage": "tsc --project tsconfig.test.json && c8 --src=src --all --exclude 'src/benchmarks/**' --exclude 'src/**/*.ts' --report --reporter html --reporter text --per-file --skip-full -- node --test 'src/tests/*.test.js'",
    "prepare": "npm run clean && tsc --project tsconfig.json && npm run lint && oxfmt --check 'src/**/*.ts' '!src/**/*.d.ts' && npm run coverage",
    "format": "oxfmt 'src/**/*.ts' '!src/**/*.d.ts'",
    "bench:bun": "bun src/benchmarks/all.bench.ts",
    "bench:node": "node --experimental-strip-types src/benchmarks/all.bench.ts",
    "verify:v8": "node --allow-natives-syntax --expose-gc --experimental-strip-types src/benchmarks/all.bench.ts",
    "profile:cpu:node": "node --prof --expose-gc --experimental-strip-types",
    "profile:heap:node": "node --heap-prof --expose-gc --experimental-strip-types",
    "profile:cpu:bun": "bun --cpu-prof --cpu-prof-md",
    "profile:heap:bun": "bun --heap-prof --heap-prof-md",
    "profile:mem:bun": "MIMALLOC_SHOW_STATS=1 bun",
    "postpack": "npm run clean"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "c8": "^9.1.0",
    "mitata": "^0.1.0",
    "oxfmt": "^0.43.0",
    "oxlint": "^1.58.0",
    "typescript": "^6.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
